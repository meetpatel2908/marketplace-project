
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Marketplace</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body>

<%- include('./new-navbar') %>


<div  id="main-content" class="container mt-4">
    <h2>Edit Product</h2>
    <form action="/edit/<%= product.id %>" method="POST" enctype="multipart/form-data">
        <div class="mb-1 w-50 mx-auto">
            <label for="category" class="form-label">Category</label>
            <select class="form-select form-select-sm" name="category" <%= product.category %> required>
                <option value="electronics">Electronics</option>
                <option value="home_essentials">Home Essentials</option>
                <option value="mobiles">Mobiles</option>
                <option value="instruments">Instruments</option>
                <option value="fashion">Fashion</option>
                <option value="books">Books</option>
                <option value="sports">Sports</option>
            </select>
        </div>
        
        <div class="mb-1 w-50 mx-auto">
            <label>Name:</label>
            <input type="text" class="form-control" name="name" value="<%= product.name %>" required>
        </div>
        <div class="mb-1 w-50 mx-auto">
            <label>Price:</label>
            <input type="number" class="form-control" name="price" value="<%= product.price %>" required>
        </div>
        <div class="mb-1 w-50 mx-auto">
            <label for="condition" class="form-label">Condition</label>
            <select class="form-select" name="condition" <%= product.description %> required>
                <option value="new">New</option>
                <option value="used">Used</option>
            </select>
        </div>
        <div class="mb-1 w-50 mx-auto">
            <label>Description:</label>
            <textarea class="form-control" name="description" required><%= product.description %></textarea>
        </div>
        <div class="mb-1 w-50 mx-auto ">
            <label>Image:</label>
            <input type="file" class="form-control " name="image">
            <img  src="<%= product.image %>" width="100">
        </div>
        <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-primary">Upload Item</button>
        </div>
    </form>
</div>




</body>
</html>

